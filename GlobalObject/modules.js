const names = require("./people");
// const data = require("./global");

//🔽 making all the data should be on one line
console.log(names.people, names.age);

// 🔽 making the data should be on different lines
// console.log(names);
